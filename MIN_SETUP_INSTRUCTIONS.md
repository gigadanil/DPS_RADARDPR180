# Минимум на ПК: подробная инструкция

Ниже два варианта. Выберите тот, который подходит вашей ситуации.

## Вариант A: только облако (без локальной разработки)
Подходит, если все уже развернуто в Supabase и вы не правите код.

Нужно на ПК:
- Любой современный браузер

Пошагово:
1) Откройте Supabase Dashboard и войдите в проект.
2) Для SQL используйте раздел SQL Editor.
3) Для Edge Functions используйте Functions UI.
4) После внесения изменений нажимайте Run или Deploy.

Примечание: если все уже в облаке, ПК можно выключать — сервисы продолжают работать в Supabase.

## Вариант B: локальная разработка + деплой в облако
Нужно, если вы хотите обновлять Edge Functions и запускать SQL-скрипты из проекта.

Минимум на ПК:
- Node.js LTS
- Supabase CLI
- Git (опционально, если храните проект в репозитории)

---

**🚨 ВНИМАНИЕ: PowerShell ≠ Node.js**

- **PowerShell** — это командная строка Windows, где вводятся команды `npm`, `supabase`, `git`
  - Приглашение: `PS C:\Users\...>`
- **Node.js REPL** — это интерактивная консоль JavaScript (запускается командой `node`)
  - Приглашение: `>` и сообщение "Welcome to Node.js"
  
**Если вы случайно попали в Node.js:**
- Нажмите `Ctrl + C` два раза или введите `.exit`

Все команды ниже выполняются **только в PowerShell**!

---

### 1) Установка инструментов

**⚠️ ВАЖНО: Как открыть PowerShell (не Node.js!)**

Все команды ниже нужно выполнять в **PowerShell**, а не в Node.js REPL!

**Как открыть PowerShell:**
- Нажмите `Win + X` → выберите "Windows PowerShell" или "Терминал"
- Или: нажмите `Win + R` → введите `powershell` → Enter

**Если вы случайно открыли Node.js** (видите `> ` и сообщение "Welcome to Node.js"):
- Нажмите `Ctrl + C` два раза, чтобы выйти
- Или введите `.exit` и нажмите Enter

---

**🚨 КАК КОПИРОВАТЬ КОМАНДЫ:**

❌ **НЕПРАВИЛЬНО** (не копируйте тройные кавычки и слово powershell):
```
```powershell
node --version
```
```

❌ **НЕПРАВИЛЬНО** (не вводите слово "powershell" перед командой):
```
powershell
node --version
```

✅ **ПРАВИЛЬНО** (копируйте только саму команду, одну строку):
```
node --version
```

**Визуальный пример:**

Вы видите в инструкции:
```powershell
node --version
```

Вы вводите в PowerShell (БЕЗ слова powershell, БЕЗ кавычек):
```
PS C:\> node --version
v24.13.1
```

**Еще примеры правильного ввода:**
- Видите: `npm --version` → Вводите: `npm --version` (и Enter)
- Видите: `git --version` → Вводите: `git --version` (и Enter)
- Видите: `supabase login` → Вводите: `supabase login` (и Enter)

**ЗАПОМНИТЕ:**
1. Откройте PowerShell ОДИН РАЗ
2. Вводите команды ПО ОДНОЙ
3. НЕ пишите слово `powershell` перед командой
4. НЕ копируйте тройные кавычки ``` и слово powershell

---

## 🔓 ВАЖНО: Разблокируйте PowerShell ПЕРЕД установкой

**⚠️ Если вы видите ошибку "выполнение сценариев отключено" — выполните это ОДИН РАЗ:**

Это нормальная защита Windows. Разблокировка нужна для работы npm.

**Пошаговая инструкция:**

1. Закройте текущий PowerShell
2. Нажмите **Win + X** → выберите **"Windows PowerShell (администратор)"**  
   (или ПКМ по Пуск → "Терминал (Администратор)")
3. В окне администратора введите эту команду (БЕЗ тройных кавычек):
   ```
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
4. Введите **Y** (латинская буква) и нажмите Enter
5. Дождитесь сообщения об успехе
6. Закройте окно администратора
7. Откройте обычный PowerShell

✅ Теперь все команды `npm` будут работать без ошибок!

---

#### 1.1) Установите Node.js LTS

**Способ 1: Официальный установщик (рекомендуется для начинающих)**
1. Перейдите на [nodejs.org](https://nodejs.org/)
2. Нажмите кнопку "LTS" (Long Term Support)
3. Скачайте установщик для Windows (.msi)
4. Запустите установщик и следуйте инструкциям
5. **После установки закройте все окна PowerShell и откройте новое!**
6. Проверьте установку (копируйте команды по одной, без кавычек):
   ```powershell
   node --version
   ```
   ```powershell
   npm --version
   ```

**Способ 2: Через Scoop (для опытных)**
```powershell
scoop install nodejs-lts
```

#### 1.2) Установите Supabase CLI

**⚠️ ВАЖНО: npm install -g supabase больше НЕ РАБОТАЕТ!**

Supabase CLI больше не поддерживает установку через `npm install -g`. Используйте Scoop.

---

**Способ 1: Через Scoop (рекомендуется)**

Если Scoop ещё не установлен:
```
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
iwr -useb get.scoop.sh | iex
```

Затем установите Git (требуется для добавления bucket):
```
scoop install git
```

Добавьте bucket Supabase и установите CLI:
```
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

**Проверка установки:**
```
supabase --version
```

Должна появиться версия, например: `2.75.0`

**Способ 2: Через npx (без установки)**

Если не хотите устанавливать CLI, используйте npx для разовых команд:
```
npx supabase login
npx supabase link --project-ref YOUR_REF
npx supabase functions deploy telegram-auth
```

#### 1.3) Установите Git (опционально)

**Способ 1: Официальный установщик**
1. Перейдите на [git-scm.com](https://git-scm.com/download/win)
2. Скачайте установщик для Windows
3. Запустите установщик (можно оставить все настройки по умолчанию)
4. После установки откройте **новое окно PowerShell** и проверьте:
   ```powershell
   git --version
   ```

**Способ 2: Через Scoop**

Откройте PowerShell и выполните:
```powershell
scoop install git
```

### 2) Авторизация и привязка проекта

**Все команды выполняются в PowerShell (не в Node.js)!**

1) Откройте **PowerShell** (Win + X → Windows PowerShell).
2) Перейдите в папку проекта:
   ```powershell
   cd "C:\Users\10\Desktop\Новая папка (12)"
   ```
3) **ВАЖНО: Обновите PATH в текущей сессии:**
   ```powershell
   $env:Path = [System.Environment]::GetEnvironmentVariable("Path","User") + ";" + [System.Environment]::GetEnvironmentVariable("Path","Machine")
   ```
4) Войдите в Supabase:
   ```powershell
   supabase login
   ```
   **Нажмите Enter** когда увидите сообщение "Press Enter to open browser"
   
   Откроется браузер для входа в ваш аккаунт Supabase.

5) Привяжите проект:
   ```powershell
   supabase link --project-ref <PROJECT_REF>
   ```

Где взять PROJECT_REF:
- Supabase Dashboard → Settings → General → Project Ref.

### 3) Деплой Edge Functions

**Выполняется в PowerShell (не в Node.js)!**

1) Убедитесь, что папки функций есть в `supabase/functions/`.
2) Выполните деплой каждой функции:
   ```powershell
   supabase functions deploy telegram-auth
   supabase functions deploy marker-delete
   ```

### 4) Запуск SQL-скриптов
Вариант через Dashboard:
1) Откройте Supabase Dashboard.
2) Перейдите в SQL Editor.
3) Вставьте нужный скрипт и нажмите Run.

Вариант через файл:
1) Откройте нужный .sql файл в проекте.
2) Скопируйте содержимое в SQL Editor.
3) Нажмите Run.

### 5) Проверка после изменений
1) Откройте таблицы в Supabase Database.
2) Проверьте, что данные и правила доступа применились.
3) При необходимости пересмотрите RLS-политики и роли.

## Права доступа
- Админ (ты, 5118431735): Может все (удалять, менять любые метки и сообщения).
- Пользователь: Может изменять/удалять только свои записи.
- Остальные: Могут только смотреть.

## Если выключить ПК
Если все уже развернуто в Supabase, то сервисы продолжают работать в облаке. Ваш ПК можно выключать.

## Что НЕ нужно устанавливать
- Сервер на ПК
- Постоянно включенный компьютер
- Локальную базу, если все уже в облаке

## Как настроить права доступа

### Вариант 1: ANON-режим (полный доступ, небезопасно)
1) Откройте Supabase Dashboard.
2) Перейдите в SQL Editor.
3) Вставьте скрипт из файла `supabase_setup.sql`.
4) Нажмите Run и дождитесь завершения.

**Важно**: этот режим дает роли `anon` полный доступ ко всем таблицам. Небезопасно для продакшена.

### Вариант 2: Безопасные RLS политики (рекомендовано)
Используйте файл `security_rls_own.sql` для настройки:
- Пользователи могут менять/удалять только свои записи
- Админ (5118431735) может удалять/менять любые записи
- Остальные могут только смотреть

**Требования**:
1) Настроенный Supabase Auth (пользователи должны логиниться)
2) Telegram логин через Edge Function (выдает JWT токен)
3) В клиенте передавайте JWT в заголовках запросов

📖 **Подробная инструкция по настройке**: см. файл `TELEGRAM_AUTH_SETUP.md`

**Быстрый запуск RLS политик**:
1) Откройте Supabase Dashboard → SQL Editor.
2) Вставьте содержимое `security_rls_own.sql`.
3) Нажмите Run.
4) Затем отдельным запуском примените `sos_karma_bonus.sql` (добавляет серверное начисление `+50` за подтвержденную SOS-помощь).
5) Если включаете Telegram-уведомления взаимопомощи — примените `telegram_help_subscribers.sql`.

### Telegram-бот для взаимопомощи (интерактивные карточки)

1) В Supabase Edge Functions добавьте секреты:
   - `TELEGRAM_HELP_BOT_TOKEN` — токен отдельного Telegram-бота уведомлений
   - `SERVICE_ROLE_KEY` — service role key проекта
   - `JWT_SECRET` — секрет JWT (тот же, что у `telegram-auth`)
   - `MINIAPP_URL` — ссылка, которую открывает кнопка «📍 Посмотреть на карте» (например `https://t.me/DPSRADARDPR180bot`)

2) Задеплойте функцию:
   - `supabase functions deploy help-notify`
   - `supabase functions deploy help-subscribe`

2.1) Подключите webhook Telegram для авто-подписки:
   - Секрет (опционально, но лучше): `TELEGRAM_HELP_WEBHOOK_SECRET`
   - Установите webhook (подставьте токен и секрет):
   - `https://api.telegram.org/bot<TELEGRAM_HELP_BOT_TOKEN>/setWebhook?url=https://phazbjchjhsruwalddis.supabase.co/functions/v1/help-subscribe&secret_token=<TELEGRAM_HELP_WEBHOOK_SECRET>`

2.2) Команды для пользователей в Telegram:
   - `/start` — включить уведомления (авто-добавление в БД)
   - `/stop` — отключить уведомления
   - `/radius 15` — изменить радиус уведомлений в км

3) Добавьте получателей в таблицу `telegram_help_subscribers`:
   - `chat_id` — ID чата пользователя с ботом
   - `radius_km` — радиус оповещений (по умолчанию 10 км)
   - `home_lat/home_lon` — опорная точка для фильтрации по радиусу (опционально)

После этого при метках взаимопомощи (`SOS`, `ДТП`, `Опасность`, `Пробка`, `Работы`) бот отправляет карточку в Telegram с кнопкой перехода в карту.
