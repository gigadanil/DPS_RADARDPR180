# MONOLITH 180 — автономная рация (24/7) без ПК

Ниже самый простой вариант: деплой Node.js сервера рации на Render (Docker).

## 0) Что получится
- Сервер рации работает 24/7 в облаке.
- В `final.html` нужно указать URL сервера в `PTT_SERVER_URL`.

## 1) Залить код в GitHub
1. Создай репозиторий.
2. Залей текущую папку проекта (включая `ptt-server/`).

## 2) Деплой на Render
1. Зайди на https://render.com
2. New → **Blueprint**
3. Подключи GitHub репозиторий
4. Render увидит `render.yaml` (в корне репозитория) и создаст сервис `monolith-180-ptt`

После деплоя Render даст URL вида:
- `https://monolith-180-ptt.onrender.com`

## 3) Подключить в приложении
В `final.html` найди:
- `const PTT_SERVER_URL = '';`

И поставь:
- `const PTT_SERVER_URL = 'https://monolith-180-ptt.onrender.com';`

Либо без правки файла (быстрее):
- открой приложение → DevTools Console →
- `localStorage.setItem('ptt:serverUrl','https://monolith-180-ptt.onrender.com')`
- `location.reload()`

## 4) Важно про бесплатный план
- На free‑плане сервис может "засыпать" при простое. Первый вход после простоя будет с задержкой.
- Файлы в `uploads/` на некоторых хостингах могут быть **временными** (после перезапуска пропадут). Для продакшена лучше S3/Cloudflare R2.

## 5) Быстрый тест
- Открой сайт с `final.html`
- Нажми 🎙️ → удерживай → отпусти
- С другого телефона — то же самое: должно приходить сразу.
