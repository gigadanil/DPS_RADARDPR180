# 📱 Android APK & Система «Свой-Чужой»

## 🎯 Что грядет?

Скоро выйдет **полноценное APK-приложение для Android**. Это будет:
- ⚡ **Стабильнее** — прямая интеграция с Android API
- 🚀 **Быстрее** — нет overhead веб-браузера
- 📲 **Всегда под рукой** — прямо на рабочем столе устройства

---

## 🔐 Система «Свой-Чужой» — Защита от ботов и мусора

Мы внедрили умную систему идентификации пользователей, чтобы:
- ✅ Автоматически определять легитимных лидеров сообщества
- ✅ Блокировать спам, фейк-метки и мусор
- ✅ Предотвращать DDoS-атаки и парсинг карты ботами
- ✅ Сохранять справедливость рейтинга и голосов

---

## 📝 Как это работает?

### 1️⃣ Для тех, кто в Telegram (Mini App)

**Если вы открываете «Монолит» прямо внутри Телеги:**

```
Ничего не меняется ✨
```

- Система **автоматически узнает вас** по Telegram ID
- Подтягивает ваш профиль и все настройки
- Никаких кнопок жать не надо
- **Зашли — и поехали!** 🚀

**За кулисами:**
```
1. Telegram Mini App отправляет initData (подписанный токен)
2. Сервер проверяет подпись и Telegram ID
3. Профиль мгновенно загружается
4. Все права доступа активны
```

---

### 2️⃣ Для тех, кто скачал APK (приложение)

**При первом запуске приложения (One-Time Setup):**

```
Один раз подтвердить → Навсегда запомнить ✅
```

#### Что нужно сделать?

1. **Скачиваешь APK и запускаешь приложение**
   - На экране появляется кнопка: `🔗 ПРИВЯЗАТЬ К TELEGRAM`

2. **Нажимаешь кнопку**
   - Открывается бот в Телеге (или браузер с ботом)
   - Нажимаешь `/start` или кнопку подтверждения

3. **Сервер связывает APK ↔ Telegram ID**
   - Создается Device Token (уникальный идентификатор устройства)
   - Записывается в БД под твоим аккаунтом
   - Система запомнит это устройство **навсегда**

4. **Возвращаешься в APK**
   - Приложение автоматически синхронизируется
   - **Мгновенный вход** — все права активны
   - Можешь работать as usual

#### После первой привязки:

```
При каждом открытии APK:
├─ Отправляется Device Token
├─ Сервер мгновенно верифицирует устройство
└─ Вход выполнен (никаких дополнительных действий)

Никогда больше 2FA, логинов, кодов! ✨
```

---

## 🔄 Логика проверки "Свой-Чужой"

### Для Telegram Mini App:
```
initData (подписано Telegram)
    ↓
Проверка подписи на серверe
    ↓
Если подпись валидна → Telegram ID = СВОЙ ✅
Если отсутствует/невалидна → ЧУЖОЙ ❌
```

### Для Android APK:
```
Device Token + Device Metadata (UUID, Android Build, etc.)
    ↓
Поиск в БД по Device Token
    ↓
Если найден и привязан к User ID → СВОЙ ✅
Если не найден или привязан кому-то другому → ЧУЖОЙ ❌
```

### Для Web-версии (browser):
```
Если есть Telegram Web Auth Cookie:
    ├─ Проверка подписи → СВОЙ ✅
Если нет:
    ├─ Может быть анонимным (limited access) или ЧУЖОЙ ❌
```

---

## 🛡️ Что блокируется для "ЧУЖИХ"?

- ❌ Создание новых меток
- ❌ Голосование (лайки, дизлайки)
- ❌ Комментарии / Отзывы
- ❌ Доступ к персональным достижениям
- ❌ Сохранение предпочтений (темы, настройки)
- ❌ Real-time обновления (только раз в 5+ минут)

**Зато видят:**
- ✅ Основная карта с метками
- ✅ Информация о метках (кто/когда/где)
- ✅ Статистика (просмотры, рейтинг)

---

## 📲 Как скачать APK?

### Варианты:

#### 1. От Google Play Store (когда выйдет):
```
Google Play → Поиск "EASY RIDE 180" → Установить → Готово
```

#### 2. Direct Download (если Play Store заблокирует):
```
https://monolith-app.ru/downloads/easy-ride-180.apk
```

#### 3. Через Telegram бота:
```
/getapk → Отправляем ссылку на скачивание
```

---

## ⚙️ Технические детали

### Device Token Generation (при привязке APK):

```javascript
// В APK:
const deviceToken = generateUUID(); // или используем Android ANDROID_ID
const metadata = {
  platform: "android",
  app_version: "1.0.0",
  android_sdk: 33,
  device_model: "Pixel 7",
  uuid: deviceToken,
  timestamp_created: Date.now()
};

// Отправляем на сервер:
POST /api/device/register
{
  verificationCode: "123456", // Получено от бота
  metadata: metadata
}
```

### Сервер проверяет:

```sql
-- При каждом запросе из APK:
SELECT * FROM user_devices 
WHERE device_token = ? AND user_id = ?;

-- Если найдено → запрос разрешен
-- Если не найдено → 401 Unauthorized
```

---

## 🔄 Миграция с Web на APK

### Если уже используешь Web-версию:

```
Шаг 1: Скачать APK
    ↓
Шаг 2: Запустить APK → Кнопка "ПРИВЯЗАТЬ К TELEGRAM"
    ↓
Шаг 3: Подтвердить в боте
    ↓
Шаг 4: Все твои данные волшебно появляются в APK ✨
    ├─ Статистика
    ├─ Достижения
    ├─ Настройки
    ├─ История
    └─ Профиль
```

**Все синхронизируется в реальном времени!**
- Если добавишь метку в APK → появляется в Web
- Если получишь достижение в Web → видно в APK

---

## 📊 Статистика и Мониторинг

### Для модераторов/администраторов:

```
Dashboard:
├─ Количество СВОИХ (верифицированных пользователей)
├─ Количество ЧУЖИХ (зашли через браузер без auth)
├─ Активные Device Tokens
├─ Попытки несанкционированного доступа
└─ Подозрительная активность (DDoS, spam, etc.)
```

---

## 🚀 Что дальше?

| Что | Когда | Статус |
|-----|-------|--------|
| Android APK Release | Q1-Q2 2026 | 🔄 Разработка |
| System "Свой-Чужой" | При релизе APK | 🔄 Ready |
| iOS App | Q3 2026 | 📋 Планируется |
| Веб-версия | Всегда | ✅ Live |
| Desktop App (Electron) | Q4 2026 | 📋 Планируется |

---

## ❓ FAQs

**Q: Если я потеряю телефон, что делать?**  
A: Напиши боту `/unregister_device` — удалим Device Token. Потом можно привязать новое устройство.

**Q: Могу ли я использовать одного аккаунта на двух телефонах?**  
A: Да! Просто привяжи оба устройства через бота. Device Token = отпечаток устройства, аккаунт = один.

**Q: Почему система не распознала меня?**  
A: Попробуй:
1. Перезагрузи приложение
2. Убедись, что привязка произошла (проверь бота `/my_devices`)
3. Очистить кэш приложения
4. Переустановить APK

**Q: Можно ли отключить "Свой-Чужой" систему?**  
A: Нет. Это основной механизм защиты карты. Но можно использовать Web-версию с ограниченным доступом.

**Q: APK хранит мои данные?**  
A: Только локально (кэш, настройки). Все критичные данные синхронизируются с сервером.

---

## 📞 Поддержка

Если что-то не работает:
- **Telegram:** [@MonolithSupportBot](https://t.me/MonolithSupportBot)
- **Email:** support@monolith-app.ru
- **GitHub Issues:** [easy-ride-180/issues](https://github.com/easy-ride-180/issues)

---

**Версия:** 1.0  
**Последнее обновление:** 19 февраля 2026 г.  
**Статус:** 🟢 Актуально
